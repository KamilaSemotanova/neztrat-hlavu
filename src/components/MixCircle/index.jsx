import React, { useState } from 'react';
import './style.css';
import { MixResult } from '../MixResult';

export const MixCircle = (mixList) => {
  // console.log(mixList.mixList);
  return (
    <>
      <div className="mixCircle"></div>
    </>
  );
};
